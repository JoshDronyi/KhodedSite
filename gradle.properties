kotlin.code.style=official

# Gradle daemon configuration - optimized for performance while maintaining stability
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=512m -XX:+UseG1GC -Dfile.encoding=UTF-8
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.workers.max=4

# Kotlin configuration - aligned with documentation standards
kotlin.daemon.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=384m -Dfile.encoding=UTF-8
kotlin.incremental=true
kotlin.incremental.multiplatform=true
kotlin.incremental.js.klib=true

# Compose configuration
org.jetbrains.compose.experimental.uikit.enabled=true

# Build optimizations
kotlin.mpp.stability.nowarn=true
kotlin.mpp.androidSourceSetLayoutVersion=2

# Network resilience configuration
systemProp.http.socketTimeout=120000
systemProp.http.connectionTimeout=120000
systemProp.https.socketTimeout=120000
systemProp.https.connectionTimeout=120000

# Kotlin/JS configuration  
kotlin.js.yarn.lockFileDirectory=build/js
kotlin.incremental.js.ir=true
